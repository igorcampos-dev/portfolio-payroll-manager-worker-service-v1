server.port=${SERVER_PORT:80}

spring.application.name=contra-cheque
spring.security.auth.signature=TESTE

spring.datasource.url=jdbc:mysql://${DB_SERVER:localhost}:${DB_PORT:3306}/dev?allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:KBf9]xEjq~K}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

spring.aws.credentials.serviceEndpoint=${AWS_ENDPOINT:http://0.0.0.0:3000}
spring.aws.credentials.accessKey=${AWS_ACESS_KEY:S3RVER}
spring.aws.credentials.secretKey=${AWS_SECRET_KEY:S3RVER}
spring.aws.credentials.region=${AWS_REGION:us-east-1}
spring.aws.credentials.bucketName=${AWS_BUCKETNAME:funcionarios}

enterprise.credential.url=jdbc:mysql://${DB_SERVER:localhost}:${DB_PORT:3306}/principal?allowPublicKeyRetrieval=true
enterprise.credential.username=${DB_USER:root}
enterprise.credential.password=${DB_PASSWORD:KBf9]xEjq~K}
